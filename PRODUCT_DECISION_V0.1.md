# 个人 AI 工作台 · 产品决定 v0.1

## 当前决定

根据补充的真实生活场景，改为先做一个手机优先、同时能在工作电脑浏览器打开的 Web App / PWA。

- 当前 demo：使用普通 HTML、CSS 和 JavaScript，零依赖，先验证手机与浏览器交互。
- 近期产品形态：响应式 PWA，通过同一个网址在手机和电脑使用。
- 权威数据：继续保存在 Obsidian Vault 的 Markdown 中。
- 当前不伪装已完成：真正的 AI 和跨设备同步。
- 下一项架构决定：同步是否值得引入一个最小、托管式服务。

### 决定为什么改变

最初假设主要设备是家中 Windows 电脑，因此优先考虑桌面 App。用户随后明确：白天主要使用手机，工作电脑不能安装应用但能使用浏览器，家中电脑通常只在晚上使用。新信息使手机优先 PWA 更符合真实场景。保留这个变更记录，避免把先前假设误写成稳定偏好。

## 三种形式分别是什么意思

### 桌面 App

像普通 Windows 软件一样从开始菜单或桌面图标打开。它可以在用户明确授权的目录中直接读写本地文件，适合连接现有 Vault。

它仍然适合未来承担本地归档和深度整理：

- 本地 Markdown 可以继续作为唯一权威版本。
- 核心功能离线可用，不需要维护线上服务。
- 界面仍可使用网页技术制作，未来不必推倒重来。

但它不适合作为当前唯一前台：白天手机和工作电脑无法方便使用。代价还包括制作安装包，并在功能变化时重新安装新版本。

### 手机 PWA

PWA 本质是可以安装到手机桌面的网页，也可以直接在普通浏览器打开。体验可以接近 App；离线界面可以通过缓存实现，但不同设备之间的数据不会因此自动同步。

它最符合当前生活场景：手机负责随时记录，工作电脑可以通过网址访问，家中电脑用于晚间回顾。需要正视的代价是 HTTPS 托管、身份验证和同步边界。

### 网页仪表盘

通过网址访问的工作台，也就是用户所说的 B/S 使用方式。PWA 是它的增强形态。只做界面和单设备本地存储可以很便宜甚至免费；一旦要让手机、工作电脑和家中电脑看到同一份私人数据，就需要某种同步服务、身份验证、备份和安全维护。

它适合多人或多设备产品，但目前会显著增加维护面。

### 微信小程序

微信小程序适合在手机上快速打开，也可以使用云开发降低自己维护服务器的工作量。但它不能自然覆盖“工作电脑普通浏览器访问”的场景；发布、平台审核、微信能力适配和云端数据边界也会形成额外维护。

如果未来发现绝大多数捕获都发生在微信里，可以把小程序做成一个轻入口，连接同一份同步数据；不建议让它成为唯一权威客户端。

## 是否需要“运营”

只给自己使用的工作台不需要用户增长、客服或内容运营。需要承担的是轻量技术维护：

- 继续保护 Vault 和原始数据备份。
- 发现真实摩擦后再改界面。
- 偶尔重新生成和安装新版本。
- 引入新依赖或联网能力时重新检查隐私边界。

如果未来公开给别人使用，才会出现真正的产品运营、兼容性、发布、服务稳定性和用户支持成本。

## 零月费边界

以下能力可以保持零月费：

- 本地界面与本地文件存储。
- 今日任务、领域管理、回顾和检索。
- 从 Markdown 生成统计与导航。
- 使用本机脚本创建和更新 Daily。

以下能力可能产生费用或额外维护：

- 调用在线大模型 API。
- 云端跨设备同步与远程备份。
- 公网托管私人数据。
- 向应用商店发布或进行代码签名。

这些能力应当彼此独立、默认关闭，而不是成为工作台运行的前提。

## v0.1 成功标准

在封装桌面应用前，先用当前 demo 判断：

1. 是否愿意每天打开一次。
2. “今天”页面是否能在两分钟内完成。
3. 六个领域中哪些真的经常使用。
4. 快速捕获最终应该进入 Inbox、Daily 还是独立待办。
5. 哪些 AI 协作确实减少了思考和整理成本。

只有这些问题有了真实答案，才开始实现自动读写 Markdown。
