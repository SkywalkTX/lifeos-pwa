{
  "name": "lifeos-pwa",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs tests/*.test.mjs",
    "check": "node --check data-model.js && node --check ai-provider.js && node --check app.js && node --check service-worker.js && node --check gateway/contracts.mjs && node --check gateway/prompts.mjs && node --check gateway/providers.mjs && node --check gateway/worker.mjs && npm test"
  }
}
